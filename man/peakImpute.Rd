% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peakImpute.R
\name{peakImpute}
\alias{peakImpute}
\title{Impute missing and low-intensity values}
\usage{
peakImpute(x, threshold, fraction, replace_low, replace_na)
}
\arguments{
\item{x}{Data frame. First column contains peak IDs, followed by abundance
columns. Any additional annotation columns (e.g. m.z, RT) are preserved.}

\item{threshold}{Numeric. Values less than or equal to this threshold are
considered undetected. Default = 1000.}

\item{fraction}{Numeric. Fraction of the minimum detected value used for
imputation. Default = 0.1.}

\item{replace_low}{Logical. Replace values <= threshold. Default = TRUE.}

\item{replace_na}{Logical. Replace NA values. Default = TRUE.}
}
\value{
A data frame with imputed abundance values, preserving original row and column layouts.
}
\description{
Replaces missing values and/or values below a specified threshold with a
fraction of the minimum detected abundance for each peak.

The replacement value is calculated independently for each peak as:
\code{min(valid values) * fraction}
where valid values are those greater than \code{threshold}.
}
\examples{
# Example data setup assumed (neg dataset)
# imputed_data <- peakImpute(neg, threshold = 1000, fraction = 0.1)

}
