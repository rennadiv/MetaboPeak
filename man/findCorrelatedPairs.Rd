% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/findCorrelatedPairs.R
\name{findCorrelatedPairs}
\alias{findCorrelatedPairs}
\title{Find and rank highly correlated peak pairs within RT windows}
\usage{
findCorrelatedPairs(x, y = NULL, n, rt_window = 0.05, cor_threshold = 0.85)
}
\arguments{
\item{x}{Data frame. Format must be: Peak identifiers in column 1, followed by \code{n} sample columns, "m.z", and "RT".}

\item{y}{Data frame (optional). A second data frame with compatible dimensions to \code{x} (e.g., negative mode). If \code{NULL}, defaults to \code{y = x}.}

\item{n}{Numeric. The exact number of sample abundance columns present in the dataset(s).}

\item{rt_window}{Numeric. The maximum allowable retention time difference (in minutes) between two peaks to be compared. Default is \code{0.05}.}

\item{cor_threshold}{Numeric. The minimum Spearman correlation coefficient required to report a pair. Default is \code{0.85}.}
}
\value{
A data frame sorted by correlation strength, showing the IDs, m/z values, RT values, RT differences, and correlation scores of matching pairs.
}
\description{
Automatically scans a dataset (or compares two datasets) to find pairs of peaks that elute
within a shared Retention Time (RT) window and exhibit a strong Spearman correlation. This
is highly useful for identifying related metabolic features like adducts, isotopes, or fragments.
}
\examples{
# findCorrelatedPairs(x = pos, y = neg, n = 48, rt_window = 0.04, cor_threshold = 0.90)
# findCorrelatedPairs(x = pos, n = 48, rt_window = 0.04, cor_threshold = 0.90)

}
